# ═══════════════════════════════════════════════════════════════════════════
# Kebab POS — Python print server
# Установка: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════════

# ─── HTTP API (порт 9999) ──────────────────────────────────────────────────
Flask>=3.0.0
flask-cors>=4.0.0

# ─── WebSocket hub для LAN-синхронизации (порт 9998) ───────────────────────
flask-sock>=0.7.0

# ─── Поиск Wi-Fi принтеров через Bonjour/mDNS ──────────────────────────────
zeroconf>=0.130.0

# ─── Печать на системные (USB) принтеры — только для Windows ───────────────
pywin32>=306 ; sys_platform == "win32"

# ─── Опционально: если понадобится SNMP-обнаружение ────────────────────────
# pysnmp>=6.0.0

# ─── Опционально: если будешь использовать nmap для сканирования ───────────
# python-nmap>=0.7.1

websocket-client>=1.7.0